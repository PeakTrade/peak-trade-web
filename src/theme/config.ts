import { ThemeConfig } from '@chakra-ui/react';

export const config: ThemeConfig = {
  initialColorMode: 'light',
  useSystemColorMode: true,
};

export const COLOR_MODE_STORAGE_KEY = 'peak-trade-color-mode';
